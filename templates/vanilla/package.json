{
    "name": "sprig-project",
    "type": "module",
    "scripts": {
        "build": "tsup src/index.ts --format esm",
        "watch": "tsup src/index.ts --watch --format esm"
    },
    "devDependencies": {
        "@repo/config": "*",
        "sprig": "^1.1.3",
        "tsup": "^8.2.4"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
