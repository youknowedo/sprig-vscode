{
    "name": "@repo/starter",
    "type": "module",
    "dependencies": {
        "@repo/config": "*"
    },
    "scripts": {
        "build": "tsup src/index.ts --format esm",
        "dev": "tsup src/index.ts --watch --format esm"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "sprig": "^1.1.3",
        "tsup": "^8.2.4"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
