{
    "name": "sprig-template",
    "displayName": "Sprig Template",
    "description": "A template for creating Sprig games with TypeScript.",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "tsup src/index.ts --format esm",
        "watch": "tsup src/index.ts --watch --format esm"
    },
    "devDependencies": {
        "@repo/config": "*",
        "@types/vscode-webview": "^1.57.5",
        "sprig": "^1.1.3",
        "tsup": "^8.2.4"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
